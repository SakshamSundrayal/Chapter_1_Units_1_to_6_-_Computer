public class qqUnitVI {
    public static void main() {
        // Sample variables
        int a = 5, b = 4, c = 3;
        
        // Question 1
        
        // double output = (1.0 / Math.pow(a, 2)) + (2.0 / Math.pow(b, 2)) + (3.0 / Math.pow(c, 2));
        // System.out.println(output);
    
        // Question 2
         /*
               int m = 3;
               System.out.printf("%d, %d, and %d forms a Pythagorean Triplet", (2 * m), (int)(Math.pow(m,2) - 1), (int)(Math.pow(m,2) + 1));
         */
        
        // Question 3
            /*  
               System.out.println("Square Root " + (Math.sqrt(a)));
               System.out.println("Cube Root " + (Math.cbrt(a)));
           */
        // Question 4
            /*
            System.out.println("Enter radius of sphere : ");
             double r = sc.nextInt(); // radius of sphere
             double v = (4.0/3.0) * (22.0/7.0) * Math.pow(r, 3);
         */
        
        // Question 5
        
        /*
           System.out.println("Enter the angles A & B");
           double output = (Math.tan(A) - Math.tan(B)) / (1 + Math.tan(A)*Math.tan(B));
           
           System.out.println(output);
         */
        
        // Question 6
        /*
          System.out.println("Enter values of a, b & c :);
          double a = sc.nextDouble();
          double b = sc.nextDouble();
          double c = sc.nextDouble();
          
          double D = Math.pow(b,2) - 4 * a * c;
          System.out.println("Discriminant : " + D);
          
         */
        
        
        // Question 7
        
        /*
          int last = 31;
          int sum = Math.pow((1 + last / 2))
         */
        
        // Question 8
        
        /*
          System.out.println("Enter the Prinicple sum : ");
          double p = sc.nextDouble();
          System.out.println("Enter rates for 3 successive years :");
          double r1 = sc.nextDouble(), r2 = sc.nextDouble(), r3 = sc.nextDouble();
          
          double Amt = p * (1 + r1 / 100.0) * (1 + r2 / 100.0) * (1 + r3 / 100.0);
          double CI = Amt - p;
          System.out.println("Amount = " + Amt);
          System.out.println("Compund Interest = " + CI);
          
         */
        
        
    }
}